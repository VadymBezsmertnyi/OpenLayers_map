import { Box } from '@mui/material';

import useStyles from './DetailsFarm.styles';

const DetailsFarm = () => {
  const classes = useStyles();

  return <Box>Details farm</Box>;
};

export default DetailsFarm;
